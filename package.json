{
  "name": "combo-project",
  "version": "0.0.1",
  "private": true,
  "description": "Combined projects",
  "author": "Andy Kellerstrass",
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose combo down",
    "build": "docker-compose combo build"
  }
}
