{
  "name": "combo-project",
  "version": "0.0.1",
  "private": true,
  "description": "Combined projects",
  "author": "Andy Kellerstrass",
  "scripts": {
    "start": "docker-compose -p combo up -d",
    "stop": "docker-compose -p combo down",
    "build": "docker-compose -p combo build"
  }
}
